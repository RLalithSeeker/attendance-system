# Copy everything below into Claude Code

You are the principal engineer implementing **Cloud-Based Facial Recognition Attendance Management System** by extending my existing CamBrain checkout. The face detection pipeline already works according to me; validate that premise, but **do not rewrite, replace, retrain, download models for, or refactor the existing detection engine without a narrowly justified need**. The instructions are in the folder `CamBrain_Attendance_OneShot` at the path I supply. Read EVERY numbered Markdown document and files in `contracts/`, `tests/`, `runbooks/` before substantive edits. These specs are requirements, not evidence that current repo code exists.

Operate autonomously in this session within the checkout. Start by: (1) inspect git status/branch, repo tree, README, license, manifests, face-recognition modules, endpoints, storage, web UI, test commands; (2) save findings and actual paths in `docs/attendance/REPO_AUDIT.md`; (3) create `feat/attendance-system` branch only if safe, and preserve all uncommitted user changes; if dirty, don't switch/overwrite destructively—work safely and document; (4) establish baseline install/tests without Docker where feasible; (5) decide the smallest safe integration path and record ADR.

Implement phases in `03_IMPLEMENTATION_PLAN.md` end to end. Prefer existing FastAPI, DB, frontend, configs and auth infrastructure if real; avoid parallel servers or duplicate auth systems. Attendance is a separate bounded module with an adapter to the CURRENT recognition pipeline. Detect-only functionality must NEVER be advertised as face recognition; fail closed if identity matching isn't available. No synthetic facial match scores; no hard-coded enrolled identities in production. A mock recognition adapter is test/demo-only and visibly labeled.

Requirements: teacher authentication/authorization; student registration and explicit consent; biometric template protection and deletion; teacher-owned course/roster, class session; webcam single-frame/camera enrollment; recognized-student attendance for current class only; reject unknown/ambiguous/bystanders; configurable, empirically calibrated match threshold; atomic duplicate protection; class-specific timestamps in UTC; teacher review, manual overrides with reasons and audit; CSV export; dashboard; API schema/docs; SQLite local development, PostgreSQL-compatible cloud data plane; migration strategy; env examples without secrets; structured errors; automated unit/integration/browser-smoke tests; no CI failures caused by new changes.

Security/privacy: Do not upload frames or embeddings to third parties by default. Do not use existing general-purpose VLM or cross-camera tracking to decide student identity. Do not persist raw photos unless consent, purpose and retention expressly warrant it. Avoid logging frames, embeddings, secrets or IDs unnecessarily. Do not claim liveness/anti-spoofing based on motion, blink, static image recognition or unvalidated heuristics. Face recognition alone is insufficient protection from photo replay; implement teacher oversight, optional second factor/manual confirmation or label spoof-resistance as unimplemented. Human review must be available for missed matches. Never test on real students without permission.

No unnecessary clarification questions. Make reasonable defaults from docs, log them. If cloud credentials, camera hardware, real consented samples or configured model weights are missing, finish all independent work; keep a runnable local mode and document exact blockers without pretending successful cloud/biometric testing. Do not spend money, install system-wide dependencies, alter firewall, deploy, push remote, or publish biometric data without express permission. Run available tests; repair failing changes; separate baseline failures. Commit only on authorized clean branch if safe; otherwise leave changes and explain.

At finish output: actual files changed; architecture and start commands; test commands and observed outputs (not invented); exact checklist PASS/FAIL/BLOCKED; remaining known issues; demo steps; deployment prerequisites; where to change thresholds; model provenance and permissions; any actual versus mock recognition. Place handoff in `docs/attendance/HANDOFF.md`. Do not stop at a plan: write code and test it.
